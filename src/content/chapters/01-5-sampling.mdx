---
slug: "01-5-sampling"
title: "Sampling: Why Outputs Can Change"
navTitle: "Sampling"
chapterNumber: 1
lessonNumber: 5
parentSlug: "01-foundations"
section: "foundations"
difficulty: "beginner"
estimatedMinutes: 7
tier: "free"
prereqs: ["01-3-attention"]
characters: ["Tess"]
ogImageVariant: "foundations"
suggestedPrompts:
  - "What does temperature actually do during text generation?"
  - "When should I use low temperature vs high temperature?"
  - "What's the difference between top-p and top-k sampling?"
  - "Why does the same prompt sometimes produce different answers?"
updatedAt: "2026-05-23"
authors: ["murugadoss"]
summary: >
  Sampling settings decide how the next token is chosen from the model's probability scores.
---

The model does not write a whole answer in one motion. It predicts one token, then another, then another.

## TL;DR

- Logits are raw scores for possible next tokens.
- Temperature changes how sharp or adventurous the choice can be.
- Top-p and top-k limit the candidate set before the next token is chosen.

## From Scores To Text

Once the model has scores for possible next tokens, decoding rules decide what comes out.

Temperature changes how sharp or flat the probability distribution is. Low temperature makes the model favor high-probability tokens. Higher temperature gives lower-probability tokens more room.

Top-p sampling, also called nucleus sampling, keeps the smallest set of likely tokens whose probabilities add up to a chosen threshold. Top-k sampling keeps only the k most likely tokens.

<ELI5Card title="Choosing from a menu">
  Low temperature orders the house special again and again. Higher temperature explores the menu. Top-p and top-k decide how much of the menu is available.
</ELI5Card>

## Why The Same Prompt Can Differ

If a model has several plausible next tokens, sampling can choose different paths. After one different token, the next probability menu changes too. That is why small randomness early in an answer can lead to a noticeably different final response.

Stable tasks usually use lower randomness. Brainstorming and creative tasks can benefit from more room to explore.

<DeepDive title="Temperature, top-p, and top-k together">
  These settings interact. Temperature reshapes probabilities. Top-p and top-k filter which candidates remain. In production systems, stable tasks often use lower randomness, while brainstorming tools may use more.

  The menu analogy breaks down because the model is not choosing from fixed meals. Each generated token changes the next menu.
</DeepDive>

<DeepDive title="What logits are">
  Logits are raw scores for every token in the vocabulary. Before the model picks the next token, these scores are converted into probabilities, often with softmax. A higher logit means the model currently considers that token more likely.
</DeepDive>

## Remember

<RememberCard>
  - Generation happens token by token.
  - Temperature adjusts randomness.
  - Top-p and top-k filter the token menu.
  - Different early choices can steer the rest of the answer.
</RememberCard>
