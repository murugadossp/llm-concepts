---
slug: "01-4-context-windows"
title: "Context Windows and Token Budgets"
navTitle: "Context Windows"
chapterNumber: 1
lessonNumber: 4
parentSlug: "01-foundations"
section: "foundations"
difficulty: "beginner"
estimatedMinutes: 7
tier: "free"
prereqs: ["01-1-tokens"]
characters: ["Tess"]
ogImageVariant: "foundations"
suggestedPrompts:
  - "What actually counts toward the context window?"
  - "Why do longer prompts cost more money?"
  - "When should I use retrieval instead of stuffing everything in context?"
  - "What is a key-value cache and why does it speed up generation?"
updatedAt: "2026-05-23"
authors: ["murugadoss"]
summary: >
  The context window is the model's working page: prompt, history, retrieved text, tools, and output all compete for space.
---

A context window is not just "how much text you can paste." It is the model's working page.

## TL;DR

- The context window includes input and generated output.
- More context can help, but it costs more and can become noisy.
- Token budgets shape product design, retrieval design, and agent memory.

## Comic: Tess's Tiny Desk

<ComicStrip>
  <ComicPanel character="Tess" title="The Limited Desk">
    <SpeechBubble speaker="Tess">I need to check the recipe, write a note, and hold my tea. There's no space left!</SpeechBubble>
    **Visual action:** Tess sits at a tiny, cramped desk piled high with books, papers, and empty cups. She looks overwhelmed trying to find a spot for a single new sheet.
    **Teaching point:** The context window is like a working desk. Everything (prompts, history, outputs) competes for the same limited area.
  </ComicPanel>
  <ComicPanel character="Tess" title="Messy Desk, Slow Work">
    <SpeechBubble speaker="Tess">A bigger desk helps, but if it's messy, I still get lost looking for my pen!</SpeechBubble>
    **Visual action:** The desk expands to a larger table, but it is covered in chaotic notes. Tess spends time searching through heaps of paper.
    **Teaching point:** While large context windows exist, too much noise can cause the model to miss crucial details.
  </ComicPanel>
</ComicStrip>

## The Model's Working Page

The context window is how much text the model can consider at once. It includes your prompt, conversation history, tool results, retrieved documents, previous assistant messages, and the model's generated tokens.

A bigger context window lets the model look at more material, but it also costs more. More tokens mean more computation. Long context can also become noisy if the important details are buried.

<ELI5Card title="A desk with limited space">
  The model can work with whatever fits on the desk. A bigger desk helps, but a messy desk can still hide the note you need.
</ELI5Card>

## What Competes For Space

In a real app, context may contain:

- System instructions.
- Developer instructions.
- User messages.
- Conversation history.
- Retrieved documents.
- Tool outputs.
- The model's own answer as it is being generated.

This is why "just paste everything" is rarely the best strategy. Good LLM systems decide what deserves to be in the working page right now.

<DeepDive title="Key-value cache">
  During generation, the model produces one token at a time. The key-value cache stores attention data from earlier tokens so the model does not recompute the whole prefix from scratch for every new token.

  This makes inference faster, especially for long prompts. The tradeoff is memory: cached keys and values take space.
</DeepDive>

## Try It

Try styling a context window yourself using the simulator below. Observe how adding optional items like history or search context quickly eats up the budget!

<ContextBudgetBar />

## Remember

<RememberCard>
  - Context is shared working space.
  - Bigger context is useful but not automatically better.
  - Retrieval helps choose what enters context.
  - Agents need context discipline because task histories get messy.
</RememberCard>

export const contextQuiz = [
  {
    question: "What counts toward the context window?",
    options: [
      "Only the text you type in the prompt box",
      "Prompt, history, retrieved text, tool results, and the model's own generated output",
      "Only the model's final answer",
      "Only system instructions",
    ],
    correctIndex: 1,
    explanation:
      "Everything sharing the model's working page — input and output alike — competes for the same token budget.",
  },
  {
    question: "Why isn't a bigger context window automatically better?",
    options: [
      "Bigger context windows are always cheaper",
      "More tokens cost more to process and can bury the important details in noise",
      "Models refuse to use extra context space",
      "Context windows can only grow, never shrink",
    ],
    correctIndex: 1,
    explanation:
      "More context means more computation and more chances for the relevant detail to get lost — good context design still matters.",
  },
];

<Quiz title="Context windows: check your understanding" questions={contextQuiz} />
